{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Serie</th>
                <td>{{ voiture.Serie }}</td>
            </tr>
            <tr>
                <th>Date_Mise_En_Marche</th>
                <td>{{ voiture.DateMiseEnMarche }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ voiture.Modele }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_index') }}">back to list</a>

    <a href="{{ path('crud_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('crud/_delete_form.html.twig') }}
{% endblock %}
